{
	"name": "@elrest/edesign-runtime-ipc-typescript",
	"description": "Elrest - eDesign - Runtime - Inter Process Communication - Typescript",
	"keywords": [
		"Elrest",
		"Automations",
		"Systeme",
		"eDesign",
		"Runtime",
		"IPC",
		"Typescript",
		"Library"
	],
	"license": "Elrest Automations Systeme GMBH",
	"author": "Slavomir Sidor <slavomir.sidor@elrest.cz>",
	"version": "1.0.247",
	"main": "build/index.js",
	"types": "build/index.d.ts",
	"scripts": {
		"test": "nyc mocha",
		"start:dev": "npx nodemon --legacy-watch --config ./nodemon.json",
		"build": "yarn clean && yarn build-tsc && yarn docs",
		"build-tsc": "npx tsc",
		"build:watch": "nodemon --config nodemon.dev.json",
		"docs": "npx typedoc --options ./typedoc.json",
		"docs-readme": "npx typedoc --plugin typedoc-plugin-markdown --theme markdown --out docs/readme && npx concat-md --toc --decrease-title-levels --dir-name-as-title temp-docs > README.md && npx showdown makehtml -i README.md -o README.html && npx rimraf temp-docs",
		"publish:patch": "yarn build && yarn publish --patch --verbose",
		"clean": "npx rimraf ./docs ./build ./.build.json",
		"cover": "npm run cover:unit && npm run cover:integration && npm run cover:report",
		"cover:unit": "nyc --silent npm run test:unit",
		"cover:integration": "nyc --silent --no-clean npm run test:integration",
		"cover:report": "nyc report --reporter=lcov --reporter=text"
	},
	"dependencies": {
		"pm2": "^5.3.0",
		"uuid": "^9.0.0"
	},
	"devDependencies": {
		"@types/node": "^20.4.8",
		"@types/uuid": "^9.0.2",
		"concat-md": "^0.5.1",
		"eslint": "^8.51.0",
		"mocha": "^10.2.0",
		"nodemon": "^3.0.1",
		"nyc": "^15.1.0",
		"rimraf": "^5.0.1",
		"showdown": "^2.1.0",
		"string-width": "^6.1.0",
		"ts-node": "^10.9.1",
		"typedoc": "^0.25.3",
		"typedoc-plugin-markdown": "^3.17.1",
		"typescript": "^5.1.6"
	}
}
